<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nombre", style: "display: block" %>
    <%= form.text_field :name, required: true %>
  </div>

  <div>
    <%= form.label "Precio", style: "display: block" %>
    <%= form.text_field :price, required: true %>
  </div>

  <div>
    <%= form.label "Descripción", style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label "Vendedor", style: "display: block" %>
    <%= form.collection_select :seller_id, Seller.all, :id, :name, required: true %>
  </div>

  <div>
    <%= form.label "Carro", style: "display: block" %>
    <%= form.collection_select :car_id, Car.all, :id, :plate, required: true %>
  </div>

  <div>
    <%= form.label "Fecha", style: "display: block" %>
    <%= form.date_field :date, required: true %>
  </div>

  <div>
    <%= form.label "Imagenes" %>
    <%= form.file_field :images, multiple: true %>
  </div>

  <div>
    <%= form.submit "Crear evento" %>
  </div>
<% end %>
